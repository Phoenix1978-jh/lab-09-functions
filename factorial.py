
userNum = input("Number please: ")
userstring = int(userNum) # new variable to convert string (userNum) into a number*/


def factorial(n):
    factorNum = 1
    if n >= 1:
        for i in range(1, userstring +1):
            factorNum = factorNum * 1

print(factorial(userstring))
